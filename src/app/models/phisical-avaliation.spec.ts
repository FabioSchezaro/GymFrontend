import { PhisicalAvaliation } from './phisical-avaliation';

describe('PhisicalAvaliation', () => {
  it('should create an instance', () => {
    expect(new PhisicalAvaliation()).toBeTruthy();
  });
});
