import { SchemaConfiguration } from './schema-configuration';

describe('SchemaConfiguration', () => {
  it('should create an instance', () => {
    expect(new SchemaConfiguration()).toBeTruthy();
  });
});
